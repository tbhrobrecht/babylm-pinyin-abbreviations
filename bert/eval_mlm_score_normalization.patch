diff --git a/evaluation_pipeline/sentence_zero_shot/run.py b/evaluation_pipeline/sentence_zero_shot/run.py
index 0000000..0000000 100644
--- a/evaluation_pipeline/sentence_zero_shot/run.py
+++ b/evaluation_pipeline/sentence_zero_shot/run.py
@@
     parser.add_argument("--max_temperature", default=None, type=float, help="Maximum temperature to apply to the logits. If None, onlny the minimum temperature will be considered.")
     parser.add_argument("--temperature_interval", default=0.05, type=float, help="Step size between temperatures applied to the logits.")
     parser.add_argument("--batch_size", default=64, type=int, help="Batch size for evaluation")
     parser.add_argument("--non_causal_batch_size", default=64, type=int, help="Mini-batch size to process each batch of inputs involving masked tokens")
+    parser.add_argument("--score_normalization", choices=["sum", "mean"], default="sum", help="Aggregate token log-probabilities with a sum or per-token mean. Mean is useful for MLM pseudo-likelihood when candidates tokenize to different lengths.")
     parser.add_argument("--full_sentence_scores", action="store_true", help="Whether to use the entire sentence to calculate the sentence scores rather than just the completion. (Only implemented for EWoK)")
     parser.add_argument("--save_predictions", action="store_true", help="Whether or not to save predictions.")
     parser.add_argument("--save_item_with_unk", action="store_true", help="Save items whose sentences contain UNK tokens to a JSONL file.")
@@
-    args.output_path = args.output_dir / args.model_name / revision_name / "zero_shot" / args.backend / args.task / dataset
+    backend_dir = args.backend if args.score_normalization == "sum" else f"{args.backend}_{args.score_normalization}"
+    args.output_path = args.output_dir / args.model_name / revision_name / "zero_shot" / backend_dir / args.task / dataset
     args.output_path.mkdir(parents=True, exist_ok=True)
diff --git a/evaluation_pipeline/sentence_zero_shot/compute_results.py b/evaluation_pipeline/sentence_zero_shot/compute_results.py
index 0000000..0000000 100644
--- a/evaluation_pipeline/sentence_zero_shot/compute_results.py
+++ b/evaluation_pipeline/sentence_zero_shot/compute_results.py
@@
 import argparse
 
 DEVICE = torch.device('cuda') if torch.cuda.is_available() else torch.device('cpu')
 
 
+def aggregate_token_log_probs(values: torch.Tensor, args: argparse.ArgumentParser) -> float:
+    """Aggregate token log-probs for one candidate sentence/completion."""
+    if values.numel() == 0:
+        return 0.0
+    if getattr(args, "score_normalization", "sum") == "mean":
+        return torch.mean(values).item()
+    return torch.sum(values).item()
+
+
 def compute_results(args: argparse.ArgumentParser, model: torch.nn.Module, dataloader: DataLoader, temperatures: list[float]):
@@
                 for examples_per_batch in sentence_dict[f'{prefix}_examples_per_batch']:
                     start_idx = curr_idx
                     end_idx = curr_idx + examples_per_batch
-                    summed_log_probs.append(torch.sum(concat_temp_log_probs[start_idx:end_idx]).item())
+                    summed_log_probs.append(
+                        aggregate_token_log_probs(concat_temp_log_probs[start_idx:end_idx], args)
+                    )
                     curr_idx += examples_per_batch
                 all_log_probs[temp].append(torch.tensor(summed_log_probs))
@@
                 for examples_per_batch in sentence_dict[f'{prefix}_examples_per_batch']:
                     start_idx = curr_idx
                     end_idx = curr_idx + examples_per_batch
-                    summed_log_probs.append(torch.sum(concat_temp_log_probs[start_idx:end_idx]).item())
+                    summed_log_probs.append(
+                        aggregate_token_log_probs(concat_temp_log_probs[start_idx:end_idx], args)
+                    )
                     curr_idx += examples_per_batch
                 all_log_probs[temp].append(torch.tensor(summed_log_probs))
